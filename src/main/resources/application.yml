spring:
  data:
    mongodb:
      host: localhost
      port: 27017
      database: bookshelf

mongock:
  runner-type: "InitializingBean"
  change-logs-scan-package:
    - ru.fazlyev.bookshelf.mongock.changelog